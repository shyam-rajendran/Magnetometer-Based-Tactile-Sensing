### COM Port & Baud Rate
Ensure the COM port and the BAUD RATE matches with the one specified in the firmware, otherwise the script won't be able to parse the data. 

Run `test.py` if using the existing code for the updated matplotlib panels. 


