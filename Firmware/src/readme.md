## Environment
To use the code for visualization, use the `initial_scanning` environment and upload it while building the same environment. You'll see the CSV format output in the terminal and can change it accordingly if you wish to do so.
Once you get the code running, run the python visualization code. 


Ensure the baud rate and the COM port are same otherwise the python script won't be able to parse the data.
